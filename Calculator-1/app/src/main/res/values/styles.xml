<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColor">@color/colorTextLight</item>
        <item name="android:editTextColor">@color/colorTextLight</item>
        <item name="android:textSize">@dimen/textSize</item>
    </style>

    <style name="DefaultButton" parent="Base.TextAppearance.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_margin">0.5dp</item>
        <item name="android:background">@drawable/button_custom</item>
        <item name="android:textColor">@color/colorTextLight</item>
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="OperationButton" parent="DefaultButton">
        <item name="android:textColor">@color/colorOperation</item>
    </style>

    <style name="CancelButton" parent="DefaultButton">
        <item name="android:textColor">@color/colorCancel</item>
    </style>

    <style name="TableRow" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:gravity">center</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
    </style>

</resources>
